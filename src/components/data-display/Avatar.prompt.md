Avatar — circular user token; renders an image, or initials derived from `name` on a teal disc.

```jsx
<Avatar name="Linh Tran" />
<Avatar src="/me.jpg" name="Linh Tran" size="lg" />
```

Sizes: `sm` 28 · `md` 36 · `lg` 44.
